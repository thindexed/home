apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: home
  namespace: default
spec:
  hosts: ["app.<DOMAIN>"]
  gateways: [ "gateway-app" ]
  http:
    - match:
      - uri:
          prefix: "/home"
      rewrite:
        uri: "/"
      route:
        - destination:
            host: home
